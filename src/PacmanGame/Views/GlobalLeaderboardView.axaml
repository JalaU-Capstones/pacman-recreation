<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:PacmanGame.ViewModels"
             mc:Ignorable="d" d:DesignWidth="896" d:DesignHeight="1024"
             x:Class="PacmanGame.Views.GlobalLeaderboardView"
             x:DataType="vm:GlobalLeaderboardViewModel"
             Background="Black">

    <UserControl.Styles>
        <StyleInclude Source="/Styles/ButtonStyles.axaml" />
    </UserControl.Styles>

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Title -->
        <StackPanel Grid.Row="0"
                    Margin="0,40,0,30">
            <TextBlock Text="GLOBAL TOP 10"
                      FontSize="60"
                      FontWeight="Bold"
                      Foreground="#FFFF00"
                      HorizontalAlignment="Center"
                      FontFamily="Consolas, Courier New">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#2121FF"
                                     BlurRadius="15"
                                     OffsetX="0"
                                     OffsetY="0"/>
                </TextBlock.Effect>
            </TextBlock>
        </StackPanel>

        <!-- Scores list -->
        <Border Grid.Row="1"
                Background="#1A1A1A"
                BorderBrush="#FFFF00"
                BorderThickness="3"
                CornerRadius="10"
                Margin="100,0"
                Padding="40"
                MaxWidth="960">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Top10Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#0A0A0A"
                                   BorderBrush="#2121FF"
                                   BorderThickness="2"
                                   CornerRadius="5"
                                   Padding="20"
                                   Margin="0,5">

                                <Grid ColumnDefinitions="60,*,150">

                                    <!-- Rank -->
                                    <TextBlock Grid.Column="0"
                                              Text="{Binding Rank, StringFormat='#{0}'}"
                                              FontSize="24"
                                              FontWeight="Bold"
                                              Foreground="#FFFF00"
                                              VerticalAlignment="Center"
                                              FontFamily="Consolas, Courier New"/>

                                    <!-- Player Name -->
                                    <TextBlock Grid.Column="1"
                                              Text="{Binding ProfileName}"
                                              FontSize="24"
                                              FontWeight="Bold"
                                              Foreground="White"
                                              VerticalAlignment="Center"
                                              FontFamily="Consolas, Courier New"/>

                                    <!-- Score -->
                                    <TextBlock Grid.Column="2"
                                              Text="{Binding HighScore, StringFormat='{}{0:N0} pts'}"
                                              FontSize="22"
                                              Foreground="#00FF00"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Right"
                                              FontFamily="Consolas, Courier New"/>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                   Margin="0,30,0,40"
                   HorizontalAlignment="Center"
                   Orientation="Horizontal"
                   Spacing="20">

            <Button Classes="arcade"
                    Command="{Binding SubmitScoreCommand}"
                    IsVisible="{Binding CanSubmitScore}"
                    MinWidth="300">
                <TextBlock Text="SUBMIT MY SCORE"
                           FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=Button}}"
                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"
                           FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=Button}}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"/>
            </Button>

            <Button Classes="arcade"
                    Command="{Binding BackToLocalScoresCommand}"
                    MinWidth="300">
                <TextBlock Text="BACK TO LOCAL"
                           FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=Button}}"
                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"
                           FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=Button}}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"/>
            </Button>

        </StackPanel>

    </Grid>

</UserControl>
